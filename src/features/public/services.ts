// Backwards-compatible public-service entry point. The owner-approved source
// lives with the company content so public routes and brief preferences cannot
// drift into separate service taxonomies.
export { publicServices } from "./company-content";
